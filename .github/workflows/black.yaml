on: [push]

jobs:
  black:
    runs-on: ubuntu-latest
    name: "black"
    steps:
      - uses: davidslusser/actions_python_black@test
        with:
          src: "src"
          options: ""
          pip_install_command: "pip install .[dev]"
