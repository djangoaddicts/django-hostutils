on: [push]

jobs:
  safety:
    runs-on: ubuntu-latest
    name: "safety"
    steps:
      - uses: davidslusser/actions_python_safety@separate_pip
        with:
          options: "--debug"
          pip_install_command: "pip install -e .[dev]"
